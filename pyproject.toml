[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "tkn-genai-runtime"
version = "0.1.0"
description = "Shared structured generation runtime for Python CLIs."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Tuckn" }]
dependencies = [
  "httpx>=0.28,<1",
  "jsonschema>=4.23,<5",
  "referencing>=0.36,<1",
  "pydantic>=2.10,<3",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
azure = ["azure-identity>=1.25,<2"]

[project.scripts]
tkn-genai = "tkn_genai_runtime.cli:main"

[dependency-groups]
dev = ["pytest>=8.3,<9", "ruff>=0.11,<1", "mypy>=1.15,<2", "types-PyYAML", "types-jsonschema"]

[tool.hatch.build.targets.wheel]
packages = ["src/tkn_genai_runtime"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
