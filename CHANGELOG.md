# 変更履歴

## 0.5.0

- 通信不要のtoken概算と参考額を `Runtime.plan()` / `generate --dry-run` に追加。
- 通貨・基準日・キャッシュ方針を持つ `TokenPricing` とモデル別 `Profile.pricing`、再計算用 `estimate_cost()` を追加。
- 成功・失敗の実行記録に、計算時のtoken・単価を含む `cost_estimate` を保持。未設定・欠測・不整合は理由付きの不明として返す。
- キャッシュ書込tokenと入力の集計範囲をusageに追加。Claudeの元の入力値を保持し、計算時にキャッシュ分を扱う。
- 補助CLIにdry-run専用 `--estimate-output-tokens` を追加。予算・停止判断は引き続き利用側が担当。
- 設定スキーマ1.1.0。1.0.xの読み込みを継続し、既存設定は書き換えない。
- README・設定/API仕様・移行ガイドとコスト計算のテストを更新。

## 0.4.0

- 不正なJSON、途中終了、拒否、本文欠落、SDK後処理失敗でも、取得済みのモデル・利用量を失敗記録へ保持。
- Codex・Claude Codeの非ゼロ終了時も、読み取れる標準出力のモデル・利用量を保持。
- Azure認証オブジェクトをRuntime内で再利用し、コンテキスト管理と `close()` で解放。注入したbackend・コールバックの所有権は利用側に維持。
- 実行記録とオフライン計画にBridge版、プロファイル名、認証情報を除く生成条件のハッシュを追加。設定スキーマは1.0.0を維持。
- LiteLLMの依存境界、外部通信拒否、失敗記録、認証の再利用・解放の回帰テストを拡充。
- README・Python API・移行ガイド・実行サンプルを更新。

## 0.3.0

- README、組み込みガイド、サンプルの記載を整備。

## 0.2.0

- azure-identityを標準依存に含め、認証方法によらず `uv tool install .` でインストールできる構成へ統一。
- Ollama・Azure OpenAIの生成要求をLiteLLM Python SDK経由へ変更。CLI接続は専用アダプターを継続。
- 公開Python API、接続プロファイル、設定スキーマ1.0.0を維持。
- SDKの遅延読み込み、同梱メタデータの利用、再試行・キャッシュ・外部コールバックの抑止。
- ローカルモデルの事前確認、元の応答による完了判定・利用量記録、JSON Schema検証を維持。
- 実SDKと模擬通信による回帰テスト、外部通信を拒否したプロセスでの検証を追加。

## 0.1.0

- Codex、Claude Code、GitHub Copilot、Ollama、Azure OpenAI の共通呼び出しAPI。
- 共有接続プロファイル、設定元の表示、安全な設定作成。
- JSON Schema 検証、オフライン計画、実行情報、標準化した例外。
- 日本語 README と既存CLIの移行ガイド。
